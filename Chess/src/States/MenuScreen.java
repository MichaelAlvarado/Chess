package States;

import javax.swing.JButton;
import javax.swing.JFrame;

public class MenuScreen {

	public MenuScreen(JFrame screen) {
		setUpScreen(screen);
		JButton play = new JButton();
		screen.add(play);
	}
	private void setUpScreen(JFrame screen) {

	}
}
